<script type="text/javascript">
    youtubeMovieDataMove = () => {
        let temp = [];
        let obj = {};
        let title, rank, lastWeekRank, albumImg, singer, chartDuration, views, link;
        '<% for(let i = 0; i < result.youtubeMoviedata.data.length; i++) { %>'
            title = '<%= result.youtubeMoviedata.data[i].title %>';
            rank = '<%= result.youtubeMoviedata.data[i].rank %>';
            lastWeekRank = '<%= result.youtubeMoviedata.data[i].lastWeekRank %>';
            // albumImg = '<%= result.youtubeMoviedata.data[i].albumImg %>';
            albumImg = '/static/res/chart_image/YoutubeMovie/<%= result.youtubeMoviedata.data[i].albumImgFile %>';
            singer = '<%= result.youtubeMoviedata.data[i].singer %>';
            chartDuration = '<%= result.youtubeMoviedata.data[i].chartDuration %>';
            views = '<%= result.youtubeMoviedata.data[i].views %>';
            link = '<%= result.youtubeMoviedata.data[i].link %>';
    
            obj = {
                "title": title,
                "rank": rank,
                "lastWeekRank": lastWeekRank,
                "albumImg": albumImg,
                "singer": singer,
                "chartDuration": chartDuration,
                "views": views,
                "link": link
            }
            temp.push(obj);
        '<% } %>'
    
        // console.log(temp);
        return temp;
    }

    
    // ******처음 출력을 위한 초기 실행 함수******
    // 1번 실행해서 데이터 저장
    // tableData에서 사용하기 위한 변수
    let ejsDataYoutubeMovie = youtubeMovieDataMove();
</script>
