<script type="text/javascript">
    borderDataMove = () => {
        let temp = [];
        let obj = {};
        let title, id, content, date, hit;
        '<% for(let i = 0; i < data.length; i++) { %>'
            number = '<%= data[i].number %>';
            title = '<%= data[i].title %>';
            id = '<%= data[i].id %>';
            content = '<%= data[i].content %>';
            date = '<%= data[i].date %>';
            hit = '<%= data[i].hit %>';
    
            obj = {
                "number": number,
                "title": title,
                "id": id,
                "content": content,
                "date": new Date(date),
                "hit": hit
            }
            temp.push(obj);
        '<% } %>'
    
        // console.log(temp);
        return temp;
    }

    // 이후에 사용하는 toLocaleDateString() 함수는 date 객체에서만 사용할 수 있습니다.
    // date를 date: date 로 저장하면 String 값으로 저장이 되서
    // 해당 함수를 사용 못하기 때문에 new Data(date)로 설정해서 값을 넣어줍니다.

    
    // ******처음 출력을 위한 초기 실행 함수******
    // 1번 실행해서 데이터 저장
    // tableData에서 사용하기 위한 변수
    let ejsDataBoardList = borderDataMove();
</script>
